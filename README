Project Unity is a python program used for unit conversions. Upon entering a unit it automatically generates a list of possible unit conversions for that input. At the moment only m/s, km/h, mph, kn, m, ft, in, cm, and mm are supported.
